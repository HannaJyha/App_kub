name: Build image and update Helm chart

on:
  push:
    tags:
      - "dev-v*"

jobs:
  build-and-update-helm:
    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
      - name: Checkout app repo
        uses: actions/checkout@v4

      - name: Set variables
        run: |
          REPO_NAME=$(echo "${GITHUB_REPOSITORY##*/}" | tr '[:upper:]' '[:lower:]')
          IMAGE_TAG="${GITHUB_REF#refs/tags/}"
          FULL_IMAGE_REPO="${{ secrets.DOCKER_REGISTRY }}/${{ secrets.DOCKER_USERNAME }}/$REPO_NAME"
          echo "REPO_NAME=$REPO_NAME" >> $GITHUB_ENV
          echo "IMAGE_TAG=$IMAGE_TAG" >> $GITHUB_ENV
          echo "IMAGE_REPO=$IMAGE_REPO" >> $GITHUB_ENV

          echo "Image will be tagged as: $FULL_IMAGE_REPO:$IMAGE_TAG"

      - name: Log in to Docker registry
        uses: docker/login-action@v2
        with:
          registry: ${{ secrets.DOCKER_REGISTRY }}
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and push image
        run: |
          docker build -t $IMAGE_REPO:$IMAGE_TAG .
          docker push $IMAGE_REPO:$IMAGE_TAG

      - name: Checkout Helm repo
        uses: actions/checkout@v4
        with:
          repository: HannaJyha/Helm_Kub
          path: helm-repo
          token: ${{ secrets.HELM_REPO_PAT }}

      - name: Install yq (tool to manipulate YAML safely)
        run: |
          sudo wget -qO /usr/local/bin/yq https://github.com/mikefarah/yq/releases/download/v4.34.1/yq_linux_amd64
          sudo chmod +x /usr/local/bin/yq

      - name: Update values.yaml in Helm repo
        run: |
          cd helm-repo
          yq eval -i ".image.repository = \"${FULL_IMAGE_REPO}\" | .image.tag = \"${IMAGE_TAG}\"" values.yaml
          
          git config user.name "github-actions"
          git config user.email "actions@github.com"
          git add values.yaml
          git commit -m "ci: update image to ${FULL_IMAGE_REPO}:${IMAGE_TAG}" || echo "No changes to commit"
          git push https://x-access-token:${{ secrets.HELM_REPO_PAT }}@github.com/HannaJyha/Helm_Kub.git HEAD:main
